name: .NET Core Desktop
on:
  push:
    tags:
      - "v*"
jobs:
  build:
    strategy:
      matrix:
        configuration: [Release]

    runs-on: windows-2019  # For a list of available runner types, refer to
                             # https://help.github.com/en/actions/reference/workflow-syntax-for-github-actions#jobsjob_idruns-on

    env:
      Solution_Name: NanoTwitchLeafs.sln                         # Replace with your solution name, i.e. MyWpfApp.sln.

    steps:
    - name: Checkout
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    # Add  MSBuild to the PATH: https://github.com/microsoft/setup-msbuild
    - name: Setup-MS Build
      uses: microsoft/setup-msbuild@v1.1
    
    # Pull and Restore Nuget
    - name: Setup Nuget
      uses: Nuget/setup-nuget@v1.0.5
      
    - name: Nuget Restore
      run: nuget restore ./NanoTwitchLeafs.sln
      
    # Build Project
    - name: Build
      run: msbuild.exe ./NanoTwitchLeafs.sln /p:platform="Any CPU" /p:configuration="Release"
  
    # Zip Files
    - name: Zip Files
      uses: TonyBogdanov/zip@1.0
      with:
        args: zip - r NanoTwitchLeafs.<AssemblyVersion>.zip ./bin/release/     
    
